<snippet>
  <content><![CDATA[
bool sortByEven(const int& x, const int& y) {
    if (x & 1 && y & 1)return x < y;
    if ((x & 1))return false;
    if ((y & 1))return true;
    return x < y;
}]]></content>
  <tabTrigger>sortby</tabTrigger>
  <description>sortbyeven</description>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
   <scope >source.c++</scope > 
</snippet>